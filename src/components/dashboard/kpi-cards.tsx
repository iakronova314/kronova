'use client'
import { CalendarClock, CheckCircle2, FileScan, MessageSquareReply, Users } from 'lucide-react'
export type DashboardKpis={documents:number;completedThisMonth:number;upcomingDeadlines:number;reviewResponseRate:number;activeMembers:number}
export function KpiCards({data,loading}:{data?:DashboardKpis;loading?:boolean}){const cards=[{label:'Documentos',value:data?.documents??0,hint:'almacenados',icon:FileScan},{label:'Procesados este mes',value:data?.completedThisMonth??0,hint:'completados',icon:CheckCircle2},{label:'Vencimientos próximos',value:data?.upcomingDeadlines??0,hint:'en 30 días',icon:CalendarClock},{label:'Reseñas respondidas',value:`${data?.reviewResponseRate??0}%`,hint:`${data?.activeMembers??0} miembros activos`,icon:data?.activeMembers?Users:MessageSquareReply}];return <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map((card)=><div key={card.label} className="rounded-xl border border-border bg-card p-5"><card.icon className="size-5 text-primary"/><p className={`mt-4 text-3xl font-semibold ${loading?'animate-pulse text-muted-foreground':''}`}>{card.value}</p><p className="mt-1 text-sm font-medium text-muted-foreground">{card.label}</p><p className="text-xs text-muted-foreground/70">{card.hint}</p></div>)}</div>}
